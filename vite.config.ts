// Minimal Vite config placeholder.
// This Angular project uses the Angular CLI for building (`ng serve` / `ng build`).
// Keep a tiny Vite config so editors or tools that expect it won't fail.

import { defineConfig } from 'vite';

export default defineConfig({
  // No special plugins or test setup required for this workspace.
});